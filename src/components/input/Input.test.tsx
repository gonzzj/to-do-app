import { render, screen } from "@testing-library/react";
import Input from "./Input";

describe("Input", () => {
    it("should work as expected", () => {
        // render(<App />);
        expect(1 + 1).toBe(2);
    });
});
